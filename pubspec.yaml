name: restaurant_app
description: A new Flutter project.

publish_to: "none" 

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^2.3.1
  flutter_native_splash: ^2.1.2+1

flutter_native_splash:
  android: true
  ios: true
  color: '#F4F4F4'
  image: assets/restaurants_splash.png

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/local_restaurant.json
    - assets/restaurants_splash.png
